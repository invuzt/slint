[package]
name = "cakru_app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
# PERBAIKAN: Menghapus default-features = false agar internal Slint tidak kehilangan modul 'Box' (std)
slint = { version = "1.16", features = ["backend-android-activity-06"] }
log = "0.4"
android_logger = "0.13"

[build-dependencies]
slint-build = "1.16"

[package.metadata.android]
package = "com.cak.cakru"
label = "Cakru App"
build_targets = ["aarch64-linux-android"]
min_sdk_version = 21
# Target SDK 34 tetap dipertahankan karena ini aman dan standar modern
target_sdk_version = 34
resources = "android-res/res"

[package.metadata.android.application]
icon = "@mipmap/ic_launcher"

[[package.metadata.android.uses_permission]]
name = "android.permission.INTERNET"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.release.package."*"]
opt-level = "z"
strip = true

[package.metadata.android.signing.release]
path = "temp.keystore"
keystore_password = "temp123"
key_alias = "temp"
key_password = "temp123"
